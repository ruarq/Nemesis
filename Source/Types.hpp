#pragma once

#include <cstdint>

using byte = char;

using i32 = int32_t;

using u32 = uint32_t;

using f32 = float;

using EntityId = i32;
static constexpr EntityId INVALID_ENTITY_ID = -1;

using TileId = i32;
static constexpr TileId INVALID_TILE_ID = -1;